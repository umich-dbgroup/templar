{
 "paraphrases": [], 
 "sentence": "what is the capital of the state with the largest population ?", 
 "sentence-with-vars": "what is the capital of the state with the largest population ?", 
 "sql": "select distinct state.capital from state where state.population = ( select max ( state.population ) from state ) ;", 
 "sql-with-vars": "select distinct state.capital from state where state.population = ( select max ( state.population ) from state ) ;", 
 "variables": []
}