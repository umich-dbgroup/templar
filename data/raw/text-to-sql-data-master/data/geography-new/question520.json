{
 "paraphrases": [], 
 "sentence": "what state has the shortest river ?", 
 "sentence-with-vars": "what state has the shortest river ?", 
 "sql": "select distinct river.traverse from river where river.length = ( select min ( distinct river.length ) from river ) ;", 
 "sql-with-vars": "select distinct river.traverse from river where river.length = ( select min ( distinct river.length ) from river ) ;", 
 "variables": []
}
