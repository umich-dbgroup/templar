{
 "paraphrases": [], 
 "sentence": "how long is the shortest river in the usa ?", 
 "sentence-with-vars": "how long is the shortest river in the usa ?", 
 "sql": "select river.length from river where river.length = ( select min ( river.length ) from river ) ;", 
 "sql-with-vars": "select river.length from river where river.length = ( select min ( river.length ) from river ) ;", 
 "variables": []
}